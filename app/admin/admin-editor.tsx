"use client";

import { useEffect, useState } from "react";
import Image from "next/image";
import {
  ArrowDown, ArrowUp, ArrowUpLeft, Blocks, BookOpenText, Check, ChevronDown,
  Code2, Download, Eye, FileJson, FileText, ImagePlus, LayoutDashboard,
  Inbox, LoaderCircle, LogOut, Monitor, PackagePlus, PlayCircle, RefreshCw, Save, Settings2,
  ShieldCheck, Smartphone, Trash2, Upload, UserPlus, Users, Video,
} from "lucide-react";
import type {
  BlogPost, ContentBlock, HomeCategory, HomeTextCard, InjectionCode, PageIntro,
  PageKey, ProductItem, ServiceItem, SiteContent, SolutionItem, StepItem, TrustItem,
} from "../../lib/site-content";
import type { AdminMember } from "../../lib/admin-users";
import type { RfqStatus, RfqSubmission } from "../../lib/rfq";

const pageLabels:Record<keyof SiteContent["pages"],string>={products:"محصولات",services:"خدمات",solutions:"راهکارها",procurement:"تأمین و RFQ",about:"درباره ما",contact:"تماس",blog:"بلاگ"};
const builderPageLabels:Record<PageKey,string>={home:"صفحه اصلی",products:"محصولات",services:"خدمات",solutions:"راهکارها",procurement:"تأمین و RFQ",about:"درباره ما",contact:"تماس",blog:"بلاگ"};
const previewPaths:Record<PageKey,string>={home:"/",products:"/products",services:"/services",solutions:"/solutions",procurement:"/procurement",about:"/about",contact:"/contact",blog:"/blog"};
const blockTypeLabels:Record<ContentBlock["type"],string>={text:"مطلب متنی",image:"تصویر و مطلب",video:"ویدئو و مطلب",cta:"دعوت به اقدام"};
const tabs=[
  {id:"general",label:"تنظیمات و لوگو",icon:Settings2},
  {id:"home",label:"صفحه اصلی",icon:LayoutDashboard},
  {id:"pages",label:"سربرگ صفحات",icon:ArrowUpLeft},
  {id:"products",label:"محصولات",icon:PackagePlus},
  {id:"content",label:"خدمات و راهکارها",icon:Check},
  {id:"blog",label:"بلاگ و مقالات",icon:BookOpenText},
  {id:"rfq",label:"صندوق RFQ",icon:Inbox},
  {id:"trust",label:"مدارک و اعتماد",icon:ShieldCheck},
  {id:"builder",label:"سازنده همه صفحات",icon:Blocks},
  {id:"code",label:"تزریق کد",icon:Code2,ownerOnly:true},
  {id:"advanced",label:"ویرایش پیشرفته",icon:FileJson},
  {id:"admins",label:"مدیران",icon:Users,ownerOnly:true},
] as const;
type TabId=typeof tabs[number]["id"];
type SaveState="idle"|"saving"|"saved"|"error";

export function AdminEditor({initialContent,initialAdmins,currentAdmin,user,signOut}:{initialContent:SiteContent;initialAdmins:AdminMember[];currentAdmin:AdminMember;user:string;signOut:string}){
  const [content,setContent]=useState(initialContent);
  const [active,setActive]=useState<TabId>("general");
  const [state,setState]=useState<SaveState>("idle");
  const [builderPage,setBuilderPage]=useState<PageKey>("home");
  const [previewPage,setPreviewPage]=useState<PageKey>("home");
  const [previewDevice,setPreviewDevice]=useState<"desktop"|"mobile">("desktop");
  const [previewRevision,setPreviewRevision]=useState(0);
  const [admins,setAdmins]=useState(initialAdmins);
  const [adminForm,setAdminForm]=useState({username:"",email:""});
  const [adminMessage,setAdminMessage]=useState("");
  const [adminBusy,setAdminBusy]=useState(false);

  const chooseTab=(id:TabId)=>{
    setActive(id);
    const map:Partial<Record<TabId,PageKey>>={home:"home",products:"products",blog:"blog"};
    if(map[id])setPreviewPage(map[id]!);
  };
  const save=async()=>{setState("saving");try{const response=await fetch("/api/admin/content",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify(content)});if(!response.ok)throw new Error();setPreviewRevision(value=>value+1);setState("saved");window.setTimeout(()=>setState("idle"),2200)}catch{setState("error")}};
  const upload=async(file:File,onDone:(url:string,contentType:string)=>void)=>{setState("saving");const data=new FormData();data.append("file",file);const response=await fetch("/api/admin/upload",{method:"POST",body:data});if(!response.ok){setState("error");return}const result=await response.json() as {url:string;contentType:string};onDone(result.url,result.contentType||file.type);setState("idle")};
  const addAdmin=async()=>{setAdminBusy(true);setAdminMessage("");try{const response=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(adminForm)});const result=await response.json() as {member?:AdminMember;error?:string};if(!response.ok||!result.member)throw new Error(result.error||"خطا در افزودن مدیر");setAdmins(previous=>[...previous.filter(item=>item.email!==result.member?.email),result.member!]);setAdminForm({username:"",email:""});setAdminMessage("مدیر جدید فعال شد؛ حالا می‌تواند با ChatGPT و همین ایمیل وارد شود.")}catch(error){setAdminMessage(error instanceof Error?error.message:"خطا در افزودن مدیر")}finally{setAdminBusy(false)}};
  const deleteAdmin=async(email:string)=>{setAdminBusy(true);setAdminMessage("");try{const response=await fetch("/api/admin/users",{method:"DELETE",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const result=await response.json() as {error?:string};if(!response.ok)throw new Error(result.error||"خطا در حذف مدیر");setAdmins(previous=>previous.filter(item=>item.email!==email));setAdminMessage("دسترسی مدیر حذف شد.")}catch(error){setAdminMessage(error instanceof Error?error.message:"خطا در حذف مدیر")}finally{setAdminBusy(false)}};

  return <main className="admin-app admin-app-v3" dir="rtl">
    <aside className="admin-sidebar"><div className="admin-brand"><Image src="/assets/clinoro-mark-minimal-grey.png" alt="Clinoro" width={48} height={48} unoptimized/><div><b>CLINORO</b><small>CONTENT STUDIO V4</small></div></div><nav>{tabs.filter(tab=>!("ownerOnly" in tab&&tab.ownerOnly&&currentAdmin.role!=="owner")).map(({id,label,icon:Icon})=><button className={active===id?"active":""} onClick={()=>chooseTab(id)} key={id}><Icon size={18}/>{label}</button>)}</nav><div className="admin-user"><span>{currentAdmin.username.slice(0,1).toUpperCase()}</span><div><b>{currentAdmin.username||user}</b><small>{currentAdmin.role==="owner"?"مدیر اصلی":"مدیر محتوا"}</small></div></div><a className="admin-signout" href={signOut}><LogOut size={17}/> خروج امن</a></aside>
    <section className="admin-workspace"><header className="admin-top"><div><small>CLINORO CMS / V6</small><h1>{tabs.find(tab=>tab.id===active)?.label}</h1></div><div><a href={previewPaths[previewPage]} target="_blank" rel="noreferrer">نمایش سایت <ArrowUpLeft size={16}/></a>{active!=="admins"&&active!=="rfq"&&<button className={`admin-save ${state}`} onClick={save} disabled={state==="saving"}>{state==="saving"?<LoaderCircle className="spin" size={18}/>:state==="saved"?<Check size={18}/>:<Save size={18}/>} {state==="saving"?"در حال ذخیره":state==="saved"?"ذخیره شد":state==="error"?"خطا؛ دوباره تلاش کنید":"ذخیره تغییرات"}</button>}</div></header>
      <div className="admin-content">
        {active==="general"&&<><AdminSnapshot content={content} admins={admins.length} onOpen={chooseTab}/><Panel title="هویت، لوگو و اطلاعات تماس" text="لوگوی خاکستری جدید فعال است؛ هر زمان بخواهید می‌توانید فایل دیگری جایگزین کنید."><Grid><MediaField wide kind="image" label="لوگوی کامل سایت" value={content.general.logoUrl} onUpload={(file)=>upload(file,url=>setContent({...content,general:{...content.general,logoUrl:url}}))}/><Field label="متن جایگزین لوگو" value={content.general.logoAlt} onChange={value=>setContent({...content,general:{...content.general,logoAlt:value}})}/><Field label="نام برند" value={content.general.brand} onChange={value=>setContent({...content,general:{...content.general,brand:value}})}/><Field label="زیرعنوان برند" value={content.general.tagline} onChange={value=>setContent({...content,general:{...content.general,tagline:value}})}/><SelectField label="میزان حرکت و موشن" value={content.general.motionMode} onChange={value=>setContent({...content,general:{...content.general,motionMode:value as SiteContent["general"]["motionMode"]}})} options={[["full","پویا و کامل"],["subtle","ملایم و حرفه‌ای"],["reduced","حداقل حرکت"]]}/><Field label="شماره تماس" value={content.general.phone} onChange={value=>setContent({...content,general:{...content.general,phone:value}})}/><Field label="ایمیل" value={content.general.email} onChange={value=>setContent({...content,general:{...content.general,email:value}})}/><Field wide label="آدرس" value={content.general.address} onChange={value=>setContent({...content,general:{...content.general,address:value}})}/><TextArea wide label="متن معرفی فوتر" value={content.general.footerText} onChange={value=>setContent({...content,general:{...content.general,footerText:value}})}/><Field wide label="عنوان مرورگر و گوگل" value={content.general.metaTitle} onChange={value=>setContent({...content,general:{...content.general,metaTitle:value}})}/><TextArea wide label="توضیحات گوگل" value={content.general.metaDescription} onChange={value=>setContent({...content,general:{...content.general,metaDescription:value}})}/></Grid></Panel></>}
        {active==="home"&&<><Panel title="Hero صفحه اصلی" text="تمام تغییرات همین لحظه در پیش‌نمایش کنار صفحه دیده می‌شوند."><Grid><Field wide label="عبارت بالای تیتر" value={content.home.kicker} onChange={value=>setContent({...content,home:{...content.home,kicker:value}})}/><Field label="تیتر اصلی" value={content.home.title} onChange={value=>setContent({...content,home:{...content.home,title:value}})}/>{content.home.signals.map((signal,index)=><Field key={index} label={`پیام متحرک ${index+1}`} value={signal} onChange={value=>{const signals=[...content.home.signals];signals[index]=value;setContent({...content,home:{...content.home,signals}})}}/>)}<TextArea wide label="توضیح Hero" value={content.home.intro} onChange={value=>setContent({...content,home:{...content.home,intro:value}})}/><MediaField wide kind="image" label="تصویر Hero" value={content.home.heroImage} onUpload={(file)=>upload(file,url=>setContent({...content,home:{...content.home,heroImage:url}}))}/></Grid></Panel><Panel title="بخش داستان Clinoro"><Grid><Field wide label="عنوان" value={content.home.storyTitle} onChange={value=>setContent({...content,home:{...content.home,storyTitle:value}})}/><TextArea wide label="متن" value={content.home.storyText} onChange={value=>setContent({...content,home:{...content.home,storyText:value}})}/><MediaField wide kind="image" label="تصویر" value={content.home.storyImage} onUpload={(file)=>upload(file,url=>setContent({...content,home:{...content.home,storyImage:url}}))}/></Grid></Panel><HomeTextCollection title="نوار اعتماد و آمار" items={content.home.proofPoints} onChange={proofPoints=>setContent({...content,home:{...content.home,proofPoints}})}/><HomeCategoryCollection items={content.home.categories} onChange={categories=>setContent({...content,home:{...content.home,categories}})} upload={upload}/><HomeTextCollection title="کارت‌های خدمات صفحه اصلی" items={content.home.serviceCards} onChange={serviceCards=>setContent({...content,home:{...content.home,serviceCards}})}/><HomeTextCollection title="مراحل مسیر پروژه" items={content.home.process} onChange={process=>setContent({...content,home:{...content.home,process}})}/><Panel title="دعوت به اقدام انتهای صفحه"><Grid><Field wide label="عنوان" value={content.home.finalCta.title} onChange={title=>setContent({...content,home:{...content.home,finalCta:{...content.home.finalCta,title}}})}/><TextArea wide label="توضیح" value={content.home.finalCta.text} onChange={text=>setContent({...content,home:{...content.home,finalCta:{...content.home.finalCta,text}}})}/></Grid></Panel></>}
        {active==="pages"&&Object.entries(content.pages).map(([key,page])=><PageEditor key={key} label={pageLabels[key as keyof SiteContent["pages"]]} page={page} onFocus={()=>setPreviewPage(key as PageKey)} onChange={next=>setContent({...content,pages:{...content.pages,[key]:next}})} onUpload={(file)=>upload(file,url=>setContent({...content,pages:{...content.pages,[key]:{...page,image:url}}}))}/>) }
        {active==="products"&&<CollectionPanel title="کاتالوگ و صفحات اختصاصی محصولات" count={content.products.length} onAdd={()=>setContent({...content,products:[...content.products,{slug:`new-product-${Date.now()}`,cat:"critical",image:"/assets/patient-monitor.jpg",gallery:[],fa:"محصول جدید",en:"New Product",tag:"Medical Equipment",summary:"معرفی کوتاه محصول",brand:"چندبرندی",model:"انتخاب متناسب با پروژه",availability:"پس از بررسی موجودی",intendedUse:"کاربرد محصول را بنویسید",specs:["ویژگی اول"],technicalSpecs:[{label:"مشخصه",value:"مقدار"}],services:["مشاوره و انتخاب"],documents:[],imageCredit:"",imageSource:"",imageLicense:"",featured:false}]})}>{content.products.map((item,index)=><ProductEditor key={`${item.slug}-${index}`} item={item} index={index} onChange={next=>{const items=[...content.products];items[index]=next;setContent({...content,products:items})}} onRemove={()=>setContent({...content,products:content.products.filter((_,i)=>i!==index)})} onUpload={(file)=>upload(file,url=>{const items=[...content.products];items[index]={...item,image:url};setContent({...content,products:items})})} onDocumentUpload={(file)=>upload(file,url=>{const items=[...content.products];items[index]={...item,documents:[...item.documents,{title:file.name.replace(/\.pdf$/i,""),type:"PDF",url}]};setContent({...content,products:items})})}/>)}</CollectionPanel>}
        {active==="content"&&<><CollectionPanel title="خدمات" count={content.services.length} onAdd={()=>setContent({...content,services:[...content.services,{en:"SERVICE",title:"خدمت جدید",text:"توضیحات خدمت",list:["مورد اول"]}]})}>{content.services.map((item,index)=><ServiceEditor key={index} item={item} index={index} onChange={next=>{const items=[...content.services];items[index]=next;setContent({...content,services:items})}} onRemove={()=>setContent({...content,services:content.services.filter((_,i)=>i!==index)})}/>)}</CollectionPanel><CollectionPanel title="راهکارها" count={content.solutions.length} onAdd={()=>setContent({...content,solutions:[...content.solutions,{title:"راهکار جدید",en:"NEW SOLUTION",image:"/assets/medical-visual.jpg",text:"توضیحات راهکار"}]})}>{content.solutions.map((item,index)=><SolutionEditor key={index} item={item} index={index} onChange={next=>{const items=[...content.solutions];items[index]=next;setContent({...content,solutions:items})}} onRemove={()=>setContent({...content,solutions:content.solutions.filter((_,i)=>i!==index)})} onUpload={(file)=>upload(file,url=>{const items=[...content.solutions];items[index]={...item,image:url};setContent({...content,solutions:items})})}/>)}</CollectionPanel><CollectionPanel title="مراحل تأمین" count={content.procurementSteps.length} onAdd={()=>setContent({...content,procurementSteps:[...content.procurementSteps,{title:"مرحله جدید",text:"توضیحات مرحله"}]})}>{content.procurementSteps.map((item,index)=><StepEditor key={index} item={item} index={index} onChange={next=>{const items=[...content.procurementSteps];items[index]=next;setContent({...content,procurementSteps:items})}} onRemove={()=>setContent({...content,procurementSteps:content.procurementSteps.filter((_,i)=>i!==index)})}/>)}</CollectionPanel><Panel title="متن درباره ما"><Grid><Field wide label="عنوان" value={content.about.headline} onChange={value=>setContent({...content,about:{...content.about,headline:value}})}/>{content.about.paragraphs.map((paragraph,index)=><TextArea wide key={index} label={`پاراگراف ${index+1}`} value={paragraph} onChange={value=>{const paragraphs=[...content.about.paragraphs];paragraphs[index]=value;setContent({...content,about:{...content.about,paragraphs}})}}/>)}<MediaField wide kind="image" label="تصویر درباره ما" value={content.about.image} onUpload={(file)=>upload(file,url=>setContent({...content,about:{...content.about,image:url}}))}/></Grid></Panel><HomeTextCollection title="ارزش‌های صفحه درباره ما" items={content.about.values} onChange={values=>setContent({...content,about:{...content.about,values}})}/></>}
        {active==="blog"&&<BlogManager content={content} setContent={setContent} upload={upload}/>}
        {active==="rfq"&&<RfqInbox/>}
        {active==="trust"&&<TrustManager content={content} setContent={setContent} upload={upload} canVerify={currentAdmin.role==="owner"}/>}
        {active==="builder"&&<BlockBuilder page={builderPage} setPage={page=>{setBuilderPage(page);setPreviewPage(page)}} content={content} setContent={setContent} upload={upload}/>}
        {active==="code"&&currentAdmin.role==="owner"&&<CodeInjectionEditor content={content} setContent={setContent}/>}
        {active==="advanced"&&<AdvancedEditor content={content} setContent={setContent}/>}
        {active==="admins"&&currentAdmin.role==="owner"&&<AdminManager admins={admins} currentAdmin={currentAdmin} form={adminForm} setForm={setAdminForm} busy={adminBusy} message={adminMessage} add={addAdmin} remove={deleteAdmin}/>}
      </div>
    </section>
    <LivePreview page={previewPage} setPage={setPreviewPage} device={previewDevice} setDevice={setPreviewDevice} revision={previewRevision}/>
  </main>;
}

function Panel({title,text,children}:{title:string;text?:string;children:React.ReactNode}){return <section className="admin-panel"><header><h2>{title}</h2>{text&&<p>{text}</p>}</header>{children}</section>}
function Grid({children}:{children:React.ReactNode}){return <div className="admin-grid">{children}</div>}
function Field({label,value,onChange,wide=false}:{label:string;value:string;onChange:(value:string)=>void;wide?:boolean}){return <label className={wide?"wide":""}><span>{label}</span><input value={value} onChange={event=>onChange(event.target.value)}/></label>}
function TextArea({label,value,onChange,wide=false}:{label:string;value:string;onChange:(value:string)=>void;wide?:boolean}){return <label className={wide?"wide":""}><span>{label}</span><textarea value={value} onChange={event=>onChange(event.target.value)}/></label>}
function CodeField({label,value,onChange,placeholder}:{label:string;value:string;onChange:(value:string)=>void;placeholder:string}){return <label className="wide admin-code-field"><span>{label}</span><textarea dir="ltr" spellCheck={false} value={value} onChange={event=>onChange(event.target.value)} placeholder={placeholder}/></label>}
function SelectField({label,value,onChange,options,wide=false}:{label:string;value:string;onChange:(value:string)=>void;options:Array<[string,string]>;wide?:boolean}){return <label className={wide?"wide":""}><span>{label}</span><select value={value} onChange={event=>onChange(event.target.value)}>{options.map(([optionValue,label])=><option value={optionValue} key={optionValue}>{label}</option>)}</select></label>}
function MediaField({label,value,onUpload,kind,wide=false}:{label:string;value:string;onUpload:(file:File)=>void;kind:"image"|"video";wide?:boolean}){return <div className={`admin-image-field${wide?" wide":""}`}><span>{label}</span><div>{value&&(kind==="video"?<video src={value} controls preload="metadata"/>:<Image src={value} width={180} height={110} unoptimized alt="پیش‌نمایش"/>)}<label>{kind==="video"?<Video size={18}/>:<ImagePlus size={18}/>} انتخاب و آپلود {kind==="video"?"ویدئو":"تصویر"}<input type="file" accept={kind==="video"?"video/mp4,video/webm,video/quicktime":"image/*"} onChange={event=>{const file=event.target.files?.[0];if(file)onUpload(file)}}/></label><small>{value||"فایلی انتخاب نشده"}</small></div></div>}
function CollectionPanel({title,count,onAdd,children}:{title:string;count:number;onAdd:()=>void;children:React.ReactNode}){return <Panel title={title}><div className="admin-collection-head"><span>{count.toLocaleString("fa-IR")} مورد</span><button className="admin-add" onClick={onAdd}><PackagePlus size={17}/> افزودن مورد جدید</button></div><div className="admin-collection">{children}</div></Panel>}
function Remove({onClick}:{onClick:()=>void}){return <button className="admin-remove" onClick={onClick} aria-label="حذف"><Trash2 size={17}/></button>}
function EditorDetails({title,subtitle,index,children,onRemove,open=false}:{title:string;subtitle:string;index:number;children:React.ReactNode;onRemove:()=>void;open?:boolean}){return <details className="admin-item" open={open}><summary><span>{String(index+1).padStart(2,"0")}</span><div><b>{title}</b><small>{subtitle}</small></div><ChevronDown size={18}/></summary><Remove onClick={onRemove}/><div className="admin-item-body">{children}</div></details>}
function PageEditor({label,page,onChange,onUpload,onFocus}:{label:string;page:PageIntro;onChange:(value:PageIntro)=>void;onUpload:(file:File)=>void;onFocus:()=>void}){return <div onFocus={onFocus}><Panel title={`صفحه ${label}`}><Grid><Field label="برچسب انگلیسی" value={page.eyebrow} onChange={value=>onChange({...page,eyebrow:value})}/><Field label="عنوان" value={page.title} onChange={value=>onChange({...page,title:value})}/><TextArea wide label="توضیحات" value={page.text} onChange={value=>onChange({...page,text:value})}/><Field wide label="یادداشت روی تصویر" value={page.note} onChange={value=>onChange({...page,note:value})}/><MediaField wide kind="image" label="تصویر Hero" value={page.image} onUpload={onUpload}/><Field label="اعتبار عکاس / منبع" value={page.credit||""} onChange={value=>onChange({...page,credit:value})}/><Field label="لینک منبع تصویر" value={page.source||""} onChange={value=>onChange({...page,source:value})}/></Grid></Panel></div>}
function ProductEditor({item,index,onChange,onRemove,onUpload,onDocumentUpload}:{item:ProductItem;index:number;onChange:(value:ProductItem)=>void;onRemove:()=>void;onUpload:(file:File)=>void;onDocumentUpload:(file:File)=>void}){return <EditorDetails title={item.fa||"محصول جدید"} subtitle={item.en||item.tag} index={index} onRemove={onRemove}><Grid><Field label="نام فارسی" value={item.fa} onChange={value=>onChange({...item,fa:value})}/><Field label="نام انگلیسی" value={item.en} onChange={value=>onChange({...item,en:value})}/><Field label="آدرس انگلیسی محصول" value={item.slug} onChange={value=>onChange({...item,slug:value.toLowerCase().replace(/[^a-z0-9-]/g,"-")})}/><Field label="دسته‌بندی" value={item.cat} onChange={value=>onChange({...item,cat:value})}/><Field label="برچسب" value={item.tag} onChange={value=>onChange({...item,tag:value})}/><Field label="برند" value={item.brand} onChange={value=>onChange({...item,brand:value})}/><Field label="مدل" value={item.model} onChange={value=>onChange({...item,model:value})}/><Field label="وضعیت تأمین" value={item.availability} onChange={value=>onChange({...item,availability:value})}/><TextArea wide label="خلاصه معرفی" value={item.summary} onChange={value=>onChange({...item,summary:value})}/><TextArea wide label="کاربرد موردنظر" value={item.intendedUse} onChange={value=>onChange({...item,intendedUse:value})}/><TextArea wide label="ویژگی‌های کلیدی؛ هر خط یک مورد" value={item.specs.join("\n")} onChange={value=>onChange({...item,specs:value.split("\n").map(line=>line.trim()).filter(Boolean)})}/><TextArea wide label="مشخصات فنی؛ هر خط: عنوان | مقدار" value={item.technicalSpecs.map(spec=>`${spec.label} | ${spec.value}`).join("\n")} onChange={value=>onChange({...item,technicalSpecs:value.split("\n").map(line=>line.split("|")).filter(parts=>parts[0]?.trim()).map(parts=>({label:parts[0].trim(),value:parts.slice(1).join("|").trim()}))})}/><TextArea wide label="خدمات همراه؛ هر خط یک مورد" value={item.services.join("\n")} onChange={value=>onChange({...item,services:value.split("\n").map(line=>line.trim()).filter(Boolean)})}/><TextArea wide label="دیتاشیت و مدارک؛ هر خط: عنوان | نوع | لینک" value={item.documents.map(document=>`${document.title} | ${document.type} | ${document.url}`).join("\n")} onChange={value=>onChange({...item,documents:value.split("\n").map(line=>line.split("|").map(part=>part.trim())).filter(parts=>parts[0]).map(parts=>({title:parts[0],type:parts[1]||"PDF",url:parts.slice(2).join("|")}))})}/><DocumentField label="آپلود دیتاشیت PDF" value="" onUpload={onDocumentUpload}/><MediaField wide kind="image" label="تصویر محصول" value={item.image} onUpload={onUpload}/><Field label="اعتبار تصویر" value={item.imageCredit} onChange={value=>onChange({...item,imageCredit:value})}/><Field label="مجوز تصویر" value={item.imageLicense} onChange={value=>onChange({...item,imageLicense:value})}/><Field wide label="لینک منبع تصویر" value={item.imageSource} onChange={value=>onChange({...item,imageSource:value})}/><label className="admin-switch"><span>محصول منتخب</span><button className={item.featured?"on":""} onClick={()=>onChange({...item,featured:!item.featured})} type="button"><i/>{item.featured?"نمایش ویژه":"عادی"}</button></label></Grid></EditorDetails>}
function ServiceEditor({item,index,onChange,onRemove}:{item:ServiceItem;index:number;onChange:(value:ServiceItem)=>void;onRemove:()=>void}){return <EditorDetails title={item.title||"خدمت جدید"} subtitle={item.en} index={index} onRemove={onRemove}><Grid><Field label="برچسب انگلیسی" value={item.en} onChange={value=>onChange({...item,en:value})}/><Field label="عنوان" value={item.title} onChange={value=>onChange({...item,title:value})}/><TextArea wide label="توضیحات" value={item.text} onChange={value=>onChange({...item,text:value})}/><TextArea wide label="موارد؛ هر خط یک مورد" value={item.list.join("\n")} onChange={value=>onChange({...item,list:value.split("\n").filter(Boolean)})}/></Grid></EditorDetails>}
function SolutionEditor({item,index,onChange,onRemove,onUpload}:{item:SolutionItem;index:number;onChange:(value:SolutionItem)=>void;onRemove:()=>void;onUpload:(file:File)=>void}){return <EditorDetails title={item.title||"راهکار جدید"} subtitle={item.en} index={index} onRemove={onRemove}><Grid><Field label="عنوان" value={item.title} onChange={value=>onChange({...item,title:value})}/><Field label="عنوان انگلیسی" value={item.en} onChange={value=>onChange({...item,en:value})}/><TextArea wide label="توضیحات" value={item.text} onChange={value=>onChange({...item,text:value})}/><MediaField wide kind="image" label="تصویر" value={item.image} onUpload={onUpload}/></Grid></EditorDetails>}
function StepEditor({item,index,onChange,onRemove}:{item:StepItem;index:number;onChange:(value:StepItem)=>void;onRemove:()=>void}){return <EditorDetails title={item.title||"مرحله جدید"} subtitle="مرحله تأمین" index={index} onRemove={onRemove}><Grid><Field label="عنوان" value={item.title} onChange={value=>onChange({...item,title:value})}/><TextArea label="توضیحات" value={item.text} onChange={value=>onChange({...item,text:value})}/></Grid></EditorDetails>}

function HomeTextCollection({title,items,onChange}:{title:string;items:HomeTextCard[];onChange:(items:HomeTextCard[])=>void}){
  const update=(index:number,item:HomeTextCard)=>{const next=[...items];next[index]=item;onChange(next)};
  return <CollectionPanel title={title} count={items.length} onAdd={()=>onChange([...items,{title:"عنوان جدید",text:"توضیحات جدید"}])}>{items.map((item,index)=><EditorDetails key={`${item.title}-${index}`} title={item.title} subtitle="بخش صفحه اصلی" index={index} onRemove={()=>onChange(items.filter((_,i)=>i!==index))}><Grid><Field label="عنوان" value={item.title} onChange={title=>update(index,{...item,title})}/><TextArea label="توضیحات" value={item.text} onChange={text=>update(index,{...item,text})}/></Grid></EditorDetails>)}</CollectionPanel>;
}

function HomeCategoryCollection({items,onChange,upload}:{items:HomeCategory[];onChange:(items:HomeCategory[])=>void;upload:(file:File,onDone:(url:string,type:string)=>void)=>void}){
  const update=(index:number,item:HomeCategory)=>{const next=[...items];next[index]=item;onChange(next)};
  return <CollectionPanel title="دسته‌بندی‌های صفحه اصلی" count={items.length} onAdd={()=>onChange([...items,{title:"دسته جدید",en:"NEW CATEGORY",image:"/assets/medical-visual.jpg"}])}>{items.map((item,index)=><EditorDetails key={`${item.title}-${index}`} title={item.title} subtitle={item.en} index={index} onRemove={()=>onChange(items.filter((_,i)=>i!==index))}><Grid><Field label="عنوان فارسی" value={item.title} onChange={title=>update(index,{...item,title})}/><Field label="عنوان انگلیسی" value={item.en} onChange={en=>update(index,{...item,en})}/><MediaField wide kind="image" label="تصویر دسته" value={item.image} onUpload={file=>upload(file,url=>update(index,{...item,image:url}))}/></Grid></EditorDetails>)}</CollectionPanel>;
}

function AdminSnapshot({content,admins,onOpen}:{content:SiteContent;admins:number;onOpen:(tab:TabId)=>void}){
  const published=content.blogPosts.filter(post=>post.published).length;
  const blocks=Object.values(content.customBlocks).reduce((sum,items)=>sum+items.length,0);
  return <section className="admin-snapshot">
    <header><div><span>وضعیت محتوای سایت</span><h2>همه‌چیز از همین‌جا در کنترل شماست</h2></div><small><i/> همگام با سایت زنده</small></header>
    <div><button onClick={()=>onOpen("products")}><b>{content.products.length.toLocaleString("fa-IR")}</b><span>محصول قابل ویرایش</span></button><button onClick={()=>onOpen("blog")}><b>{published.toLocaleString("fa-IR")}</b><span>مقاله منتشرشده</span></button><button onClick={()=>onOpen("builder")}><b>{blocks.toLocaleString("fa-IR")}</b><span>بخش سفارشی</span></button><button onClick={()=>onOpen("admins")}><b>{admins.toLocaleString("fa-IR")}</b><span>مدیر فعال</span></button></div>
  </section>;
}

function BlogManager({content,setContent,upload}:{content:SiteContent;setContent:(value:SiteContent)=>void;upload:(file:File,onDone:(url:string,type:string)=>void)=>void}){
  const replace=(posts:BlogPost[])=>setContent({...content,blogPosts:posts});
  const add=()=>replace([...content.blogPosts,{id:crypto.randomUUID(),slug:`new-post-${Date.now()}`,title:"عنوان مقاله جدید",excerpt:"خلاصه کوتاه مقاله",content:"متن مقاله را اینجا بنویسید.",image:"/assets/medical-visual.jpg",category:"دانش پزشکی",author:"تیم Clinoro",publishedAt:new Date().toISOString().slice(0,10),published:false,seoTitle:"",seoDescription:"",sources:[],imageCredit:"",imageSource:""}]);
  const update=(index:number,post:BlogPost)=>{const posts=[...content.blogPosts];posts[index]=post;replace(posts)};
  return <CollectionPanel title="مدیریت بلاگ و مقالات" count={content.blogPosts.length} onAdd={add}>{content.blogPosts.map((post,index)=><EditorDetails key={post.id} title={post.title} subtitle={post.published?"منتشرشده":"پیش‌نویس"} index={index} open={index===content.blogPosts.length-1} onRemove={()=>replace(content.blogPosts.filter((_,i)=>i!==index))}><Grid><Field label="عنوان مقاله" value={post.title} onChange={value=>update(index,{...post,title:value})}/><Field label="آدرس انگلیسی (slug)" value={post.slug} onChange={value=>update(index,{...post,slug:value.toLowerCase().replace(/[^a-z0-9-]/g,"-")})}/><Field label="دسته‌بندی" value={post.category} onChange={value=>update(index,{...post,category:value})}/><Field label="نویسنده" value={post.author} onChange={value=>update(index,{...post,author:value})}/><Field label="تاریخ انتشار" value={post.publishedAt} onChange={value=>update(index,{...post,publishedAt:value})}/><label className="admin-switch"><span>وضعیت انتشار</span><button className={post.published?"on":""} onClick={()=>update(index,{...post,published:!post.published})} type="button"><i/>{post.published?"منتشر شود":"پیش‌نویس"}</button></label><TextArea wide label="خلاصه" value={post.excerpt} onChange={value=>update(index,{...post,excerpt:value})}/><TextArea wide label="متن کامل؛ ## برای تیتر، - برای فهرست و > برای نکته" value={post.content} onChange={value=>update(index,{...post,content:value})}/><MediaField wide kind="image" label="تصویر شاخص" value={post.image} onUpload={file=>upload(file,url=>update(index,{...post,image:url}))}/><Field label="اعتبار / نام عکاس" value={post.imageCredit||""} onChange={value=>update(index,{...post,imageCredit:value})}/><Field label="لینک منبع عکس" value={post.imageSource||""} onChange={value=>update(index,{...post,imageSource:value})}/><TextArea wide label="منابع مقاله؛ هر خط: عنوان | لینک" value={(post.sources||[]).map(source=>`${source.title} | ${source.url}`).join("\n")} onChange={value=>update(index,{...post,sources:value.split("\n").map(line=>line.trim()).filter(Boolean).map(line=>{const split=line.lastIndexOf("|");return split>0?{title:line.slice(0,split).trim(),url:line.slice(split+1).trim()}:{title:line,url:""}})})}/><Field wide label="عنوان SEO" value={post.seoTitle} onChange={value=>update(index,{...post,seoTitle:value})}/><TextArea wide label="توضیحات SEO" value={post.seoDescription} onChange={value=>update(index,{...post,seoDescription:value})}/></Grid></EditorDetails>)}</CollectionPanel>;
}

const rfqStatusLabels:Record<RfqStatus,string>={new:"جدید",reviewing:"در حال بررسی",contacted:"تماس گرفته شد",qualified:"واجد شرایط",closed:"بسته‌شده"};
function RfqInbox(){
  const [items,setItems]=useState<RfqSubmission[]>([]);const [busy,setBusy]=useState(true);const [error,setError]=useState("");const [filter,setFilter]=useState<"all"|RfqStatus>("all");const [reload,setReload]=useState(0);
  useEffect(()=>{let active=true;fetch(`/api/admin/rfq${filter==="all"?"":`?status=${filter}`}`,{cache:"no-store"}).then(async response=>{const result=await response.json() as {submissions?:RfqSubmission[];error?:string};if(!response.ok)throw new Error(result.error||"خطا در دریافت درخواست‌ها");if(active)setItems(result.submissions||[])}).catch(reason=>{if(active)setError(reason instanceof Error?reason.message:"خطا در دریافت درخواست‌ها")}).finally(()=>{if(active)setBusy(false)});return()=>{active=false}},[filter,reload]);
  const changeStatus=async(id:string,status:RfqStatus)=>{const response=await fetch("/api/admin/rfq",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,status})});if(response.ok)setItems(current=>current.map(item=>item.id===id?{...item,status,updatedAt:Date.now()}:item));else setError("تغییر وضعیت ذخیره نشد")};
  const exportCsv=()=>{const safe=(value:string)=>`"${(/^[=+\-@]/.test(value)?"'":"")+value.replaceAll('"','""')}"`;const rows=[["reference","created_at","status","name","organization","phone","email","topic","product","city","quantity","timeline","message"],...items.map(item=>[item.reference,new Date(item.createdAt).toISOString(),item.status,item.name,item.organization,item.phone,item.email,item.topic,item.productSlug,item.city,item.quantity,item.timeline,item.message])];const blob=new Blob(["\ufeff"+rows.map(row=>row.map(value=>safe(String(value))).join(",")).join("\n")],{type:"text/csv;charset=utf-8"});const url=URL.createObjectURL(blob);const anchor=document.createElement("a");anchor.href=url;anchor.download=`clinoro-rfq-${new Date().toISOString().slice(0,10)}.csv`;anchor.click();URL.revokeObjectURL(url)};
  return <Panel title="صندوق درخواست‌های واقعی" text="هر ارسال موفق فرم عمومی با کد پیگیری اینجا ثبت می‌شود. شماره و ایمیل فقط برای پیگیری همان درخواست استفاده شود."><div className="rfq-admin-toolbar"><label><span>فیلتر وضعیت</span><select value={filter} onChange={event=>{setBusy(true);setError("");setFilter(event.target.value as "all"|RfqStatus)}}><option value="all">همه درخواست‌ها</option>{Object.entries(rfqStatusLabels).map(([value,label])=><option value={value} key={value}>{label}</option>)}</select></label><div><button onClick={()=>{setBusy(true);setError("");setReload(value=>value+1)}}><RefreshCw size={17}/> تازه‌سازی</button><button onClick={exportCsv} disabled={!items.length}><Download size={17}/> خروجی CSV</button></div></div>{error&&<p className="admin-message error" role="alert">{error}</p>}{busy?<div className="admin-loading"><LoaderCircle className="spin"/> در حال دریافت درخواست‌ها</div>:items.length?<div className="rfq-admin-list">{items.map(item=><article key={item.id}><header><div><b>{item.name}</b><span dir="ltr">{item.reference}</span></div><time>{new Date(item.createdAt).toLocaleString("fa-IR")}</time></header><div className="rfq-admin-meta"><span>{item.organization||"بدون نام مجموعه"}</span><a href={`tel:${item.phone}`} dir="ltr">{item.phone}</a>{item.email&&<a href={`mailto:${item.email}`}>{item.email}</a>}<span>{item.city||"شهر نامشخص"}</span></div><div className="rfq-admin-topic"><b>{item.topic}</b>{item.productSlug&&<small dir="ltr">{item.productSlug}</small>}{item.quantity&&<small>تعداد: {item.quantity}</small>}{item.timeline&&<small>{item.timeline}</small>}</div><p>{item.message}</p><footer><select value={item.status} onChange={event=>void changeStatus(item.id,event.target.value as RfqStatus)}>{Object.entries(rfqStatusLabels).map(([value,label])=><option value={value} key={value}>{label}</option>)}</select>{item.sourceUrl&&<a href={item.sourceUrl} target="_blank" rel="noreferrer">صفحه مبدأ</a>}</footer></article>)}</div>:<div className="admin-empty"><Inbox size={30}/><b>هنوز درخواستی ثبت نشده است</b><span>پس از ارسال فرم عمومی، درخواست‌ها اینجا ظاهر می‌شوند.</span></div>}</Panel>;
}

function TrustManager({content,setContent,upload,canVerify}:{content:SiteContent;setContent:(value:SiteContent)=>void;upload:(file:File,onDone:(url:string,type:string)=>void)=>void;canVerify:boolean}){
  const replace=(items:TrustItem[])=>setContent({...content,trustItems:items});const update=(index:number,item:TrustItem)=>{const items=[...content.trustItems];items[index]=item;replace(items)};
  const add=()=>replace([...content.trustItems,{id:crypto.randomUUID(),type:"document",title:"مدرک جدید",subtitle:"",description:"",image:"",fileUrl:"",issuer:"",issuedAt:"",published:false,verified:false}]);
  return <CollectionPanel title="پروژه‌ها، مشتریان، گواهی‌ها و مدارک اعتماد" count={content.trustItems.length} onAdd={add}>{!canVerify&&<div className="admin-code-warning"><ShieldCheck size={22}/><div><b>تأیید نهایی با مدیر اصلی است</b><p>شما می‌توانید اطلاعات و فایل را آماده کنید؛ وضعیت تأیید و انتشار عمومی فقط توسط مالک سایت فعال می‌شود.</p></div></div>}{content.trustItems.map((item,index)=><EditorDetails key={item.id} title={item.title} subtitle={item.verified?"تأییدشده":"در انتظار تأیید"} index={index} onRemove={()=>replace(content.trustItems.filter((_,itemIndex)=>itemIndex!==index))}><Grid><SelectField label="نوع" value={item.type} onChange={value=>update(index,{...item,type:value as TrustItem["type"]})} options={[["project","پروژه"],["client","مشتری"],["certificate","گواهی"],["document","مدرک"]]}/><Field label="عنوان" value={item.title} onChange={value=>update(index,{...item,title:value})}/><Field label="زیرعنوان" value={item.subtitle} onChange={value=>update(index,{...item,subtitle:value})}/><Field label="صادرکننده / مرجع" value={item.issuer} onChange={value=>update(index,{...item,issuer:value})}/><Field label="تاریخ" value={item.issuedAt} onChange={value=>update(index,{...item,issuedAt:value})}/><TextArea wide label="توضیحات قابل انتشار" value={item.description} onChange={value=>update(index,{...item,description:value})}/><MediaField wide kind="image" label="تصویر یا لوگوی مجاز" value={item.image} onUpload={file=>upload(file,url=>update(index,{...item,image:url}))}/><DocumentField label="فایل مدرک PDF" value={item.fileUrl} onUpload={file=>upload(file,url=>update(index,{...item,fileUrl:url}))}/><label className="admin-switch"><span>تأیید اصالت</span><button disabled={!canVerify} className={item.verified?"on":""} onClick={()=>update(index,{...item,verified:!item.verified,published:item.verified?false:item.published})} type="button"><i/>{item.verified?"تأییدشده":"تأییدنشده"}</button></label><label className="admin-switch"><span>انتشار عمومی</span><button disabled={!canVerify||!item.verified} className={item.published?"on":""} onClick={()=>update(index,{...item,published:!item.published})} type="button"><i/>{item.published?"منتشر شود":"پنهان"}</button></label></Grid></EditorDetails>)}</CollectionPanel>;
}

function DocumentField({label,value,onUpload}:{label:string;value:string;onUpload:(file:File)=>void}){return <div className="admin-image-field wide"><span>{label}</span><div>{value&&<a href={value} target="_blank" rel="noreferrer"><FileText size={20}/> مشاهده فایل فعلی</a>}<label><Upload size={18}/> انتخاب و آپلود PDF<input type="file" accept="application/pdf" onChange={event=>{const file=event.target.files?.[0];if(file)onUpload(file)}}/></label><small>{value||"فایلی انتخاب نشده"}</small></div></div>}

function BlockBuilder({page,setPage,content,setContent,upload}:{page:PageKey;setPage:(page:PageKey)=>void;content:SiteContent;setContent:(content:SiteContent)=>void;upload:(file:File,onDone:(url:string,type:string)=>void)=>void}){
  const blocks=content.customBlocks[page]||[];
  const replace=(next:ContentBlock[])=>setContent({...content,customBlocks:{...content.customBlocks,[page]:next}});
  const add=(type:ContentBlock["type"])=>replace([...blocks,{id:crypto.randomUUID(),type,title:"عنوان بخش جدید",text:"متن موردنظر را اینجا بنویسید.",mediaUrl:"",caption:"",linkLabel:type==="cta"?"اطلاعات بیشتر":"",linkUrl:type==="cta"?"/contact":"",theme:"light"}]);
  const update=(index:number,next:ContentBlock)=>{const items=[...blocks];items[index]=next;replace(items)};
  const move=(index:number,direction:-1|1)=>{const target=index+direction;if(target<0||target>=blocks.length)return;const items=[...blocks];[items[index],items[target]]=[items[target],items[index]];replace(items)};
  return <><Panel title="سازنده محتوای آزاد" text="در هر صفحه هر تعداد متن، تصویر، ویدئو یا CTA بسازید و ترتیبشان را تغییر دهید."><div className="builder-toolbar"><label><span>صفحه مقصد</span><select value={page} onChange={event=>setPage(event.target.value as PageKey)}>{Object.entries(builderPageLabels).map(([key,label])=><option value={key} key={key}>{label}</option>)}</select></label><div><button onClick={()=>add("text")}><FileText size={17}/> متن</button><button onClick={()=>add("image")}><ImagePlus size={17}/> تصویر</button><button onClick={()=>add("video")}><PlayCircle size={17}/> ویدئو</button><button onClick={()=>add("cta")}><ArrowUpLeft size={17}/> دکمه و CTA</button></div></div></Panel><Panel title={`بخش‌های صفحه ${builderPageLabels[page]}`} text={blocks.length?`${blocks.length.toLocaleString("fa-IR")} بخش قابل ویرایش`:"هنوز بخشی اضافه نشده؛ از دکمه‌های بالا شروع کنید."}><div className="admin-collection">{blocks.map((block,index)=><details className="admin-item admin-block-item" open={index===blocks.length-1} key={block.id}><summary><span>{String(index+1).padStart(2,"0")}</span><div><b>{block.title||blockTypeLabels[block.type]}</b><small>{blockTypeLabels[block.type]}</small></div><ChevronDown size={18}/></summary><div className="block-order"><button onClick={()=>move(index,-1)} disabled={index===0} aria-label="انتقال به بالا"><ArrowUp size={16}/></button><button onClick={()=>move(index,1)} disabled={index===blocks.length-1} aria-label="انتقال به پایین"><ArrowDown size={16}/></button><button onClick={()=>replace(blocks.filter((_,i)=>i!==index))} aria-label="حذف بخش"><Trash2 size={16}/></button></div><div className="admin-item-body"><Grid><SelectField label="نوع بخش" value={block.type} onChange={value=>update(index,{...block,type:value as ContentBlock["type"]})} options={Object.entries(blockTypeLabels) as Array<[string,string]>}/><SelectField label="ظاهر بخش" value={block.theme} onChange={value=>update(index,{...block,theme:value as ContentBlock["theme"]})} options={[["light","روشن"],["glass","شیشه‌ای"],["dark","تیره"]]}/><Field wide label="عنوان" value={block.title} onChange={value=>update(index,{...block,title:value})}/><TextArea wide label="متن؛ برای پاراگراف جدید Enter بزنید" value={block.text} onChange={value=>update(index,{...block,text:value})}/>{(block.type==="image"||block.type==="video")&&<><MediaField wide kind={block.type} label={block.type==="video"?"فایل ویدئو":"فایل تصویر"} value={block.mediaUrl} onUpload={(file)=>upload(file,(url)=>update(index,{...block,mediaUrl:url}))}/><Field wide label="زیرنویس رسانه" value={block.caption} onChange={value=>update(index,{...block,caption:value})}/></>}{block.type==="cta"&&<><Field label="متن دکمه" value={block.linkLabel} onChange={value=>update(index,{...block,linkLabel:value})}/><Field label="لینک دکمه" value={block.linkUrl} onChange={value=>update(index,{...block,linkUrl:value})}/></>}</Grid></div></details>)}</div></Panel></>;
}

function CodeInjectionEditor({content,setContent}:{content:SiteContent;setContent:(value:SiteContent)=>void}){
  const [page,setPage]=useState<PageKey>("home");
  const updateGlobal=(next:InjectionCode)=>setContent({...content,injections:{...content.injections,global:next}});
  const updatePage=(next:InjectionCode)=>setContent({...content,injections:{...content.injections,pages:{...content.injections.pages,[page]:next}}});
  const global=content.injections.global;
  const local=content.injections.pages[page];
  return <><div className="admin-code-warning"><ShieldCheck size={22}/><div><b>دسترسی ویژه مدیر اصلی</b><p>کد تزریقی می‌تواند ظاهر و رفتار کل سایت را تغییر دهد. ابتدا در پیش‌نمایش بررسی کنید و سپس ذخیره کنید.</p></div></div><Panel title="کد سراسری سایت" text="این کد در تمام صفحات اجرا می‌شود."><Grid><CodeField label="CSS سراسری" value={global.css} onChange={css=>updateGlobal({...global,css})} placeholder="/* custom CSS */"/><CodeField label="HTML انتهای صفحه" value={global.html} onChange={html=>updateGlobal({...global,html})} placeholder="<!-- custom HTML -->"/><CodeField label="JavaScript سراسری" value={global.javascript} onChange={javascript=>updateGlobal({...global,javascript})} placeholder="// custom JavaScript"/></Grid></Panel><Panel title="کد اختصاصی هر صفحه" text="یک صفحه انتخاب کنید؛ کد فقط همان‌جا فعال می‌شود."><div className="admin-code-page"><label><span>صفحه مقصد</span><select value={page} onChange={event=>setPage(event.target.value as PageKey)}>{Object.entries(builderPageLabels).map(([key,label])=><option value={key} key={key}>{label}</option>)}</select></label></div><Grid><CodeField label="CSS صفحه" value={local.css} onChange={css=>updatePage({...local,css})} placeholder={`/* ${page} CSS */`}/><CodeField label="HTML صفحه" value={local.html} onChange={html=>updatePage({...local,html})} placeholder="<!-- page HTML -->"/><CodeField label="JavaScript صفحه" value={local.javascript} onChange={javascript=>updatePage({...local,javascript})} placeholder="// page JavaScript"/></Grid></Panel></>;
}

function AdvancedEditor({content,setContent}:{content:SiteContent;setContent:(value:SiteContent)=>void}){
  const [source,setSource]=useState(()=>JSON.stringify(content,null,2));
  const [message,setMessage]=useState("");
  const apply=()=>{try{const parsed=JSON.parse(source) as SiteContent;if(!parsed.general||!parsed.home||!Array.isArray(parsed.products))throw new Error();setContent(parsed);setMessage("ساختار اعمال شد؛ برای نهایی‌شدن دکمه ذخیره تغییرات را بزنید.")}catch{setMessage("ساختار JSON معتبر نیست.")}};
  const download=()=>{const blob=new Blob([JSON.stringify(content,null,2)],{type:"application/json"});const url=URL.createObjectURL(blob);const anchor=document.createElement("a");anchor.href=url;anchor.download="clinoro-content.json";anchor.click();URL.revokeObjectURL(url)};
  return <Panel title="کنترل کامل ساختار محتوا" text="برای بکاپ، انتقال یا تغییر گروهی تمام داده‌های سایت. اگر با JSON آشنا نیستید از بخش‌های معمولی پنل استفاده کنید."><div className="advanced-toolbar"><button onClick={()=>setSource(JSON.stringify(content,null,2))}><FileJson size={17}/> دریافت آخرین وضعیت</button><button onClick={download}><Download size={17}/> خروجی JSON</button><label><Upload size={17}/> ورود فایل JSON<input type="file" accept="application/json" onChange={async event=>{const file=event.target.files?.[0];if(file)setSource(await file.text())}}/></label><button className="primary" onClick={apply}><Check size={17}/> اعمال در ویرایشگر</button></div><textarea className="advanced-json" dir="ltr" spellCheck={false} value={source} onChange={event=>setSource(event.target.value)}/>{message&&<p className="admin-message">{message}</p>}</Panel>;
}

function AdminManager({admins,currentAdmin,form,setForm,busy,message,add,remove}:{admins:AdminMember[];currentAdmin:AdminMember;form:{username:string;email:string};setForm:(value:{username:string;email:string})=>void;busy:boolean;message:string;add:()=>void;remove:(email:string)=>void}){
  return <><Panel title="افزودن مدیر جدید" text="مدیر جدید با حساب ChatGPT و همان ایمیل وارد می‌شود. نام کاربری برای نمایش داخل پنل است و پسوردی در سایت ذخیره نمی‌شود."><div className="admin-invite"><label><span>نام کاربری / نام نمایشی</span><input value={form.username} onChange={event=>setForm({...form,username:event.target.value})} placeholder="مثلاً Sara Content"/></label><label><span>ایمیل مدیر</span><input dir="ltr" value={form.email} onChange={event=>setForm({...form,email:event.target.value})} placeholder="name@example.com" type="email"/></label><button disabled={busy||!form.email||!form.username} onClick={add}>{busy?<LoaderCircle className="spin" size={18}/>:<UserPlus size={18}/>} افزودن و فعال‌سازی</button></div>{message&&<p className="admin-message">{message}</p>}<div className="admin-permission-note"><ShieldCheck size={20}/><div><b>تفکیک دسترسی امنیتی</b><span>مدیران محتوا می‌توانند مطالب، محصولات، رسانه‌ها و RFQها را مدیریت کنند؛ افزودن مدیر، تزریق کد و تأیید مدارک فقط در اختیار مدیر اصلی است.</span></div></div></Panel><Panel title="مدیران فعال"><div className="admin-members">{admins.map(member=><article key={member.email}><span>{member.username.slice(0,1).toUpperCase()}</span><div><b>{member.username}</b><small dir="ltr">{member.email}</small></div><em>{member.role==="owner"?"مالک":"مدیر محتوا"}</em>{member.role!=="owner"&&member.email!==currentAdmin.email&&<button disabled={busy} onClick={()=>remove(member.email)} aria-label={`حذف ${member.username}`}><Trash2 size={17}/></button>}</article>)}</div></Panel></>;
}

function LivePreview({page,setPage,device,setDevice,revision}:{page:PageKey;setPage:(page:PageKey)=>void;device:"desktop"|"mobile";setDevice:(device:"desktop"|"mobile")=>void;revision:number}){
  const source=`${previewPaths[page]}?cms-preview=1&revision=${revision}`;
  return <aside className="admin-preview"><header><div><span><i/> پیش‌نمایش واقعی</span><small>نسخه ذخیره‌شده سایت</small></div><div className="preview-devices"><button className={device==="desktop"?"active":""} onClick={()=>setDevice("desktop")} aria-label="نمای دسکتاپ"><Monitor size={16}/></button><button className={device==="mobile"?"active":""} onClick={()=>setDevice("mobile")} aria-label="نمای موبایل"><Smartphone size={16}/></button></div></header><div className="preview-page-select"><select value={page} onChange={event=>setPage(event.target.value as PageKey)}>{Object.entries(builderPageLabels).map(([key,label])=><option value={key} key={key}>{label}</option>)}</select><a href={previewPaths[page]} target="_blank" rel="noreferrer"><Eye size={15}/> بازکردن</a></div><p className="preview-hint">تغییرات را ذخیره کنید تا همین نمای واقعی با صفحه عمومی به‌روز شود.</p><div className={`preview-stage ${device}`}><div className="preview-browser"><div className="preview-browser-bar"><i/><i/><i/></div><iframe key={source} src={source} title={`پیش‌نمایش واقعی ${builderPageLabels[page]}`} loading="eager"/></div></div></aside>;
}
